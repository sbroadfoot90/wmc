{{define "title"}}Editing {{.LoginInfo.Profile.Name}}{{end}}
{{define "content"}}
<form action="/edit" method="post">
	<input type="hidden" name="Id" value="{{.LoginInfo.User.ID}}" />
	
	<label for="Name">Name:</label>
	<input type="text" name="Name" value="{{.LoginInfo.Profile.Name}}" />
	
	<label for="Tagline">Tagline:</label>
	<input type="text" name="Tagline" value="{{.LoginInfo.Profile.Tagline}}" />
	
	<input type="submit" value="Edit" />
</form>
{{end}}

